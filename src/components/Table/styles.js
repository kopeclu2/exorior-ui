import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
    table: {
        minWidth: 500,
    },
    headText: {
        fontSize: '0.975rem',
        fontWeight: '700',
        color: 'rgb(51,51,51)',
    },
    tableCell: {
        fontSize: '0.9rem',
    },
});
