import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    displayFlex: {
        display: 'flex',
    },
    arrowBack: {
        width: '50%',
        marginRight: '1.115rem',
    },
}));
